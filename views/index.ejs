<!DOCTYPE html>
<html lang="en">
<head>
    <title>narenltk</title>
</head>
<body>
    <img id="image">
    aloha
    <script src="http://hanh.github.io/ha13/socket.io/3.1.0/socket.io.js"></script>
    <script>
        // const socket = io.connect('http://localhost:3030');
        const socket = io.connect('http://hanh.github.io/ha13');
        socket.on('image', (image) => {
            // console.log('data', data);
            const img = document.getElementById('image');
            img.src = `data:image/jpeg;base64,${image}`;
        });
    </script>

</body>
</html>
